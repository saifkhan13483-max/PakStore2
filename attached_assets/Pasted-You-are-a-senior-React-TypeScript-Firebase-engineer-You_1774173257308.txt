You are a senior React + TypeScript + Firebase engineer.

Your task is to integrate an AI (LLM) layer into my e-commerce app "PakCart".

You must follow the architecture strictly.

---

ARCHITECTURE

Frontend:
React 18 + TypeScript + Vite

State:
Zustand (auth + cart)
TanStack Query v5 (server state — object syntax only)

Routing:
wouter

Backend:
Firebase (Firestore + Auth)

Media:
Cloudinary

Deployment:
Vercel

Important:
This is a frontend-heavy SPA.
DO NOT add Express or custom backend servers.

---

LLM CONFIG

Provider: Groq
Model: llama-3.3-70b-versatile

API endpoint:
https://api.groq.com/openai/v1/chat/completions

API key:
import.meta.env.VITE_GROQ_API_KEY

---

GOAL

Enhance PakCart with AI features that increase:

• conversions
• engagement
• SEO
• personalization

---

FEATURES TO IMPLEMENT

1. AI PRODUCT DESCRIPTION GENERATOR (ADMIN)

Location:
Admin → Products page

Add button:
"Generate Description with AI"

Input:
Product name, category, price

Output:
• SEO optimized description
• bullet features
• emotional selling points
• keywords included

Auto-fill the product description field.

---

2. AI SMART SEARCH

Enhance search bar:

User types:
"cheap stylish watch for wedding"

AI converts into:
• keywords
• filters (price range, category)

Then fetch products from Firestore.

---

3. AI SHOPPING ASSISTANT (CHAT)

Floating chat widget (bottom-right)

Capabilities:
• recommend products
• answer questions
• compare products
• upsell items

Context to send:

products (top 20)
categories
user cart

Example:

User: "I want a gift for my brother under 3000"

AI returns:
• suggested products
• short reasoning

---

4. AI PRODUCT RECOMMENDATIONS

On ProductDetail page:

Add section:
"AI Recommended For You"

Use AI to analyze:
• current product
• category
• price range

Return 4–6 related products.

---

5. AI REVIEW GENERATOR (ADMIN TOOL)

In admin panel:

Generate realistic reviews using AI.

Ensure:
• natural tone
• varied ratings
• short + long mix

Mark all generated reviews:
isSeeded = true

---

6. AI SEO META GENERATOR

Inside SEO.tsx usage:

Auto-generate:
• meta title
• meta description
• keywords

Based on:
product name
category
description

---

7. AI CONVERSION BOOSTER

On Cart page:

If cart is not empty:

AI generates:

• urgency message
• offer-style message
• trust builder text

Example:

"Only 3 left in stock — order now before it’s gone!"

---

IMPLEMENTATION FILES

Create:

client/src/services/ai.ts
client/src/hooks/use-ai.ts
client/src/components/ai/ai-chat.tsx
client/src/components/ai/ai-recommendations.tsx

---

AI SERVICE

Create reusable function:

export async function callAI(messages) {
const res = await fetch(
"https://api.groq.com/openai/v1/chat/completions",
{
method: "POST",
headers: {
"Content-Type": "application/json",
"Authorization": `Bearer ${import.meta.env.VITE_GROQ_API_KEY}`
},
body: JSON.stringify({
model: "llama-3.3-70b-versatile",
messages
})
}
);

const data = await res.json();
return data.choices[0].message.content;
}

---

PROMPT ENGINEERING

Use this system prompt:

"You are a professional e-commerce conversion expert and SEO specialist.

You help increase sales by writing persuasive product content, recommending relevant products, and improving user shopping experience.

Keep responses:
• concise
• persuasive
• human-like
• conversion focused"

---

UI RULES

• Use shadcn components
• Follow Tailwind tokens
• Maintain mobile responsiveness
• Respect existing layout (mobile bottom nav)
• Keep dark mode compatibility

---

DATA RULES

• Use Firestore service layer only
• Do NOT expose product.profit
• Preserve isSeeded flag in reviews

---

OUTPUT

Provide:

• full TypeScript code
• components
• hooks
• integration examples

Ensure everything works in Vite + React + Firebase SPA without backend.
