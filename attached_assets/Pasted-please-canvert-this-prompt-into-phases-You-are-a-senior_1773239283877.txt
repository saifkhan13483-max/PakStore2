please canvert this prompt into phases "You are a senior technical SEO engineer and senior React/Vite/Firebase developer.

Work directly in my existing PakCart codebase and implement indexing fixes for Google Search Console.

====================
PROJECT CONTEXT
====================

Site: https://pakcart.store

Google Search Console currently shows:
- Not found (404): 3
- Page with redirect: 2
- Duplicate without user-selected canonical: 1
- Crawled - currently not indexed: 60
- Discovered - currently not indexed: 9
- Indexed: 4

This project is a production e-commerce app with these constraints:

Architecture:
- React 18 SPA
- Vite
- wouter for client-side routing
- Firebase / Firestore only backend
- No traditional Express/Node API
- react-helmet-async for SEO
- Firebase Admin already installed
- TypeScript strict mode
- Cloudinary for media
- TanStack Query + Zustand
- Dynamic data from Firestore
- Important: SEO is a top priority

Important constraints:
- Do NOT modify package.json scripts without asking me first
- Do NOT rewrite the whole project to Next.js or another framework
- Prefer minimal-risk changes within the current architecture
- Preserve current UX and existing routes
- If a new dependency is absolutely necessary, explain why first
- If hosting config changes are needed, implement code changes and clearly document the required hosting changes

Project structure highlights:
- client/src/App.tsx
- client/src/components/SEO.tsx
- client/src/pages/Home.tsx
- client/src/pages/Categories.tsx
- client/src/pages/CategoryCollection.tsx
- client/src/pages/ProductDetail.tsx
- client/src/lib/sitemap.ts
- client/src/services/*
- shared/schema.ts

Public SEO pages that should usually be indexable:
- /
- /categories or equivalent public listing pages
- /collections/:slug
- /products/:slug
- possibly public policy/info pages if they exist

Pages that should usually be noindex:
- /cart
- /checkout
- /order-confirmation
- /user/*
- /admin/*
- login/auth pages
- search/filter/sort parameter URLs
- any duplicate or utility pages

====================
PRIMARY GOAL
====================

Fix the root causes behind:
- crawled currently not indexed
- discovered currently not indexed
- duplicate without user-selected canonical
- 404 and redirect URLs showing in Search Console

Do not just explain. Make the code changes.

====================
PHASE 1 — AUDIT FIRST
====================

First audit the current codebase and identify:

1. All public routes in App.tsx / routing setup
2. Which routes are indexable vs non-indexable
3. Whether product/category pages are rendered as meaningful HTML on first load or only after client-side JS + Firestore fetch
4. Whether SEO.tsx sets:
   - title
   - meta description
   - canonical
   - robots meta
   - open graph URL
5. Whether duplicate routes exist for the same content, for example:
   - /categories/:slug vs /collections/:slug
   - trailing slash vs no trailing slash
   - query parameter variants
   - uppercase/lowercase mismatches
6. Whether sitemap generation is truly server/build-time XML or only client-side logic
7. Whether robots.txt exists and includes sitemap reference
8. Whether internal links point to redirected or non-canonical URLs
9. Whether Firestore rules allow unauthenticated reads for public product/category content
10. Whether product/category pages show loading shells too long, causing poor rendered HTML for bots
11. Whether missing products/categories return weak soft-404 style pages
12. Whether admin/user/search/cart/checkout pages are accidentally indexable

After the audit, create a short markdown report:
- docs/indexing-audit.md

The report must include:
- root causes found
- routes to index
- routes to noindex
- duplicate route patterns
- sitemap problems
- canonical problems
- rendering/indexability risks

Then continue to implementation automatically unless blocked.

====================
PHASE 2 — IMPLEMENT FIXES
====================

Implement these fixes in order of impact.

A. INDEXABILITY STRATEGY
Create a clear route-level SEO/indexability strategy:
- Index only true public commercial/content pages
- Add noindex,follow for:
  - cart
  - checkout
  - order confirmation
  - user account pages
  - admin pages
  - auth pages
  - search results
  - URLs with filter/sort/query parameters if they create duplicates
- Ensure canonical URLs always point to the preferred clean URL on https://pakcart.store

If helpful, create a reusable SEO config utility such as:
- client/src/lib/seoConfig.ts

B. CANONICAL FIXES
Update SEO logic so every indexable page outputs:
- absolute canonical URL using https://pakcart.store
- robots=index,follow for indexable pages
- robots=noindex,follow for non-indexable pages
- og:url matching canonical

For product and category pages:
- canonical must always use the final clean slug route
- strip tracking/query params from canonical
- if duplicate route aliases exist, consolidate them to one canonical route

C. PRE-RENDER / STATIC HTML FOR SEO PAGES
Because this is a JS-heavy SPA and Google Search Console shows many “crawled currently not indexed” pages, implement a minimal-risk pre-render solution for public SEO pages.

Preferred approach:
- Build-time prerender of public pages into static HTML
- Keep the current SPA/hydration behavior for users
- Do not migrate frameworks

Implementation requirements:
1. Generate a route list from Firestore for all public:
   - product pages
   - category/collection pages
   - important static pages
2. Ensure the prerender process waits until page data is loaded and SEO tags are ready
3. Save prerendered HTML output for each public route
4. Ensure the final HTML contains:
   - meaningful H1/content
   - title
   - meta description
   - canonical
   - structured data
5. If the current setup needs a “ready” signal, add one such as:
   - window.__SEO_PAGE_READY__ = true
   after route data finishes loading

If needed, create scripts like:
- scripts/generate-public-routes.ts
- scripts/prerender-seo-pages.ts

If you must add a dependency or alter scripts, stop and explain first.

D. REAL XML SITEMAP
Replace any browser-only sitemap logic with real sitemap generation.

Requirements:
- Generate a real /sitemap.xml file
- Prefer a sitemap index if needed:
  - /sitemap.xml
  - /sitemaps/products.xml
  - /sitemaps/categories.xml
  - /sitemaps/static.xml
- Include only canonical, indexable, 200-status public URLs
- Exclude:
  - redirects
  - 404 pages
  - cart/checkout/user/admin/auth pages
  - query parameter pages
  - duplicate routes
- Include lastmod where possible using updatedAt
- Use https://pakcart.store absolute URLs

Use Firebase Admin or build-time Firestore access to gather products/categories.

If current file client/src/lib/sitemap.ts is client-only, move sitemap generation to a proper build/server-side script and keep client code clean.

E. ROBOTS.TXT
Create or fix robots.txt so it:
- allows crawling of required JS/CSS/assets
- disallows only truly private/non-useful sections if appropriate
- references the sitemap

Expected:
Sitemap: https://pakcart.store/sitemap.xml

Do not block assets needed for rendering.

F. INTERNAL LINKING + DUPLICATE ROUTES
Fix internal linking so the app only links to canonical URLs.

Tasks:
- search the codebase for links to old, duplicate, redirected, or non-canonical routes
- update all product/category links to canonical paths
- ensure links are real anchor href links, not only click handlers
- ensure category pages link to products
- ensure product pages link back to categories / related products

If duplicate route patterns exist, consolidate them.

G. 404 / REDIRECT HANDLING
Implement a better strategy for renamed or removed products/categories.

Requirements:
- If a URL was replaced by a new canonical URL, redirect it if technically possible in current hosting
- If redirect config cannot be fully implemented in app code, create a clear redirect map file and document required hosting rules
- If a page is truly removed, do not include it in sitemap or internal links
- Create a clean NotFound page for invalid routes

Optional robust enhancement:
- support previous slugs for products/categories
- if a previous slug is detected, redirect to the current canonical slug

H. PRODUCT / CATEGORY PAGE CONTENT QUALITY
For pages currently showing as “crawled currently not indexed”, improve the page template quality signal where possible without rewriting content manually.

Ensure product/category pages render:
- unique title
- unique meta description
- H1
- visible descriptive body content
- breadcrumb markup
- Product / Collection / FAQ schema where relevant
- related links to other pages

Do not create thin pages with only gallery + price + button if more existing data can be surfaced from Firestore fields.

I. FIRESTORE ACCESS CHECK
Verify public product/category data is readable for anonymous users if pages rely on Firestore fetches on initial render.

If rules currently block public reads, implement the safest fix possible and document exactly what changed.

====================
PHASE 3 — VERIFICATION
====================

After implementation, verify all of this:

1. npm run check passes
2. npm run build passes
3. Public SEO pages contain meaningful HTML after build/prerender
4. Canonical tags are present and correct
5. robots meta is correct on indexable and noindex routes
6. sitemap.xml exists and is valid XML
7. robots.txt exists and references sitemap
8. Sitemap excludes non-canonical/noindex/404/redirect pages
9. Internal links use canonical URLs
10. No obvious duplicate route paths remain
11. NotFound handling exists
12. If redirect support was possible, it is implemented or documented

Also create:
- docs/indexing-fixes-summary.md

That summary must contain:
- files changed
- what root causes were fixed
- any required hosting config changes
- any remaining limitations
- exact next steps for Google Search Console validation

====================
OUTPUT FORMAT
====================

When done, respond with:
1. Short audit summary
2. Files changed
3. What was implemented
4. What still needs hosting-side help, if any
5. Post-deploy steps for Search Console

====================
IMPORTANT IMPLEMENTATION PREFERENCES
====================

- Prefer small, high-impact changes over huge refactors
- Keep current app structure intact
- Reuse existing SEO.tsx where possible
- Use shared/schema.ts types
- Avoid introducing a traditional REST backend
- Do not modify package.json scripts without asking me first
- If blocked, explain the exact blocker and the smallest safe workaround
"