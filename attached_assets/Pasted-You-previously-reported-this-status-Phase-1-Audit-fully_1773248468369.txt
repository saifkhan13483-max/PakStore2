You previously reported this status:

- Phase 1 Audit: fully done
- Phase 2A Indexability strategy: done
- Phase 2B Canonical fixes: done
- Phase 2C Pre-render/static HTML: only half done
- Phase 2D Real XML sitemap: done
- Phase 2E robots.txt: done
- Phase 2F Internal linking: done
- Phase 2G 404/redirect handling: partial
- Phase 2H Page content quality: done
- Phase 2I Firestore access: done
- Phase 3 Verification: claimed fully done

I need you to stop relying on documentation-only claims and do a PROOF-BASED verification plus completion of the missing work.

====================
GOAL
====================

1. Verify which phases are ACTUALLY implemented in code and build output.
2. Finish the remaining gaps.
3. Provide evidence, not just statements.

Do not just say “done”.
Do not just update markdown docs.
I want code-level proof, build-level proof, and artifact-level proof.

====================
PROJECT RULES
====================

- Existing app: React 18 + Vite + wouter + Firebase/Firestore
- No framework migration
- Keep current architecture
- Do NOT rewrite to Next.js
- Keep changes minimal-risk
- Do NOT modify package.json scripts unless absolutely necessary
- If a dependency is required for prerendering, explain why and then add the smallest safe dependency
- Reuse existing SEO.tsx and current route structure where possible

====================
PHASE A — PROOF-BASED STATUS CHECK
====================

Create a new report:
- docs/indexing-implementation-proof.md

For EACH item below, mark one of:
- FULLY IMPLEMENTED
- PARTIALLY IMPLEMENTED
- NOT IMPLEMENTED

And for each item provide:
- exact file paths
- exact functions/components/scripts involved
- evidence from code
- evidence from build output if applicable
- what is missing if partial

Check these one by one:

1. Route-level indexability strategy
2. Canonical tag generation
3. robots meta generation
4. og:url generation
5. Real prerender output for public pages
6. Real sitemap.xml generation
7. robots.txt generation
8. Internal links using canonical URLs
9. 404 handling
10. Redirect handling for renamed/legacy product/category URLs
11. Product/category page content quality for SEO
12. Public anonymous access to Firestore-backed product/category content
13. Verification claims in Phase 3

Important:
- If prerender output does not actually exist in build artifacts, mark prerender as PARTIAL or NOT IMPLEMENTED.
- If redirect map / previous slug redirect does not exist, mark redirect handling as PARTIAL.

Then continue automatically to fix any partial or missing items.

====================
PHASE B — COMPLETE THE REMAINING GAPS
====================

The highest priority is Phase 2C: make prerendering ACTUALLY WORK end-to-end.

Implement a real prerender pipeline for public SEO pages:
- homepage
- key static pages
- /collections/:slug
- /products/:slug

Requirements:
1. Generate the full list of public SEO routes from Firestore/build data
2. Start from the existing architecture
3. Use the existing __SEO_PAGE_READY__ approach if already added
4. Wait until route data, title/meta/canonical, and visible content are ready
5. Output real prerendered HTML files into the build output
6. Ensure generated HTML contains:
   - title
   - meta description
   - canonical
   - robots meta
   - H1
   - meaningful visible content
   - structured data where applicable

If Puppeteer is required:
- you are allowed to add it
- explain exactly why
- keep the dependency change minimal
- implement the prerender script properly
- actually run it and confirm HTML files were produced

If another smaller/more reliable approach is better in this codebase, use it, but explain why.

====================
PHASE C — COMPLETE REDIRECT HANDLING
====================

Finish Phase 2G properly.

Implement a robust redirect strategy for renamed or changed product/category URLs.

Preferred approaches:
- previousSlugs support on product/category documents, OR
- a redirect map file, OR
- both

Requirements:
1. If a legacy slug is requested, redirect to the canonical current slug if technically possible
2. If host-level redirect config is required, create the needed config/artifact and document it clearly
3. Exclude removed/invalid URLs from sitemap
4. Ensure internal links never point to legacy or redirected URLs
5. Keep a clean NotFound page for truly invalid routes

Create if needed:
- docs/hosting-redirects.md
- redirects/redirect-map.json
- helper utilities for previous slug lookup

====================
PHASE D — REAL VERIFICATION
====================

After all fixes, run real verification.

You must actually run and confirm:
1. npm run check
2. npm run build
3. prerender step
4. existence of sitemap.xml
5. existence of robots.txt

Then inspect the output and provide proof for:
- homepage HTML
- at least 2 collection pages
- at least 3 product pages

For each sampled page, confirm that built/prerendered HTML includes:
- <title>
- meta description
- canonical
- robots meta
- H1
- visible body content

Also verify:
- noindex on cart/checkout/admin/user/auth pages
- sitemap excludes non-indexable and redirected URLs
- canonical URLs use https://pakcart.store
- query params are stripped from canonical URLs

====================
OUTPUT REQUIRED
====================

When you finish, respond with this exact structure:

1. FINAL STATUS MATRIX
   - each phase/subtask marked FULL / PARTIAL / NOT IMPLEMENTED
   - with one-line explanation each

2. FILES CHANGED
   - full list

3. WHAT YOU IMPLEMENTED
   - concise but specific

4. PROOF
   - command results
   - generated artifact paths
   - sample prerendered route outputs
   - sample sitemap/robots evidence

5. HOSTING-SIDE CHANGES STILL NEEDED
   - only if applicable

6. NEXT STEPS FOR DEPLOY + GOOGLE SEARCH CONSOLE
   - exact actions after deploy

====================
IMPORTANT
====================

Do not tell me Phase 3 is “fully done” unless prerendering is actually functioning and verified.
Do not rely on docs as proof.
Code, build output, and generated files are the source of truth.
