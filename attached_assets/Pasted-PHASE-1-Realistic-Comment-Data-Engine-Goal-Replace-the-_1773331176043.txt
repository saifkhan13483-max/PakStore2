PHASE 1: Realistic Comment Data Engine
Goal: Replace the current static arrays of names/reviews with an intelligent, product-aware comment generation system.

PHASE 1 PROMPT:

CONTEXT: [You are working on an existing e-commerce site called PakStore (project: pakstore-45ec7).
It is a pure client-side Vite + React + TypeScript app using Firebase (Firestore, Auth),
Cloudinary for images, Zustand for state, and wouter for routing.

There is NO backend server. All logic runs client-side or via CLI scripts using Firebase Admin SDK.

The existing seed comments system lives in:
- client/src/lib/seed-comments.ts (frontend logic)
- scripts/seed-comments.ts (CLI script)
- client/src/pages/Home.tsx (admin trigger button)

Current comment schema in Firestore "comments" collection:
{
  productId: string,
  userName: string,
  content: string,
  rating: number (currently only 4 or 5),
  userId: "system-seed",
  userPhoto: string,
  createdAt: Timestamp,
  updatedAt: Timestamp
}

Products collection has: averageRating, reviewCount fields that get recalculated after seeding.

IMPORTANT CONSTRAINTS:
- Keep userId: "system-seed" convention for all seeded comments (cleanup ability)
- Must work in both frontend (admin button) and CLI script contexts
- No backend/Cloud Functions — everything client-side or CLI
- All Firebase config for CLI is hardcoded in scripts/ for project pakstore-45ec7
- Maintain backward compatibility with existing comment rendering components]

TASK: Upgrade the seed comment DATA QUALITY to be realistic and product-aware.

CURRENT PROBLEM:
- Names are generic (random from a small pool)
- Review text is generic ("Great product!", "Love it!") — not product-specific
- Ratings are ONLY 4 or 5 (unrealistic — real stores have 3s and occasional 2s)
- All comments feel identical regardless of product type
- No variation in review length or style
- Timestamps are all identical (seeded at same moment)

REQUIREMENTS:

1. PRODUCT-AWARE REVIEW CONTENT:
   Create a review template system that generates content based on product attributes.
   - Read the product's `name`, `category`, `price`, and `description` from Firestore
   - Create review template pools organized by PRODUCT CATEGORY. For example:
     - Electronics: mention battery life, build quality, screen, performance, value for money
     - Clothing/Fashion: mention fabric quality, fit, color accuracy, sizing, comfort
     - Home/Kitchen: mention durability, ease of use, looks in the house, packaging
     - Beauty/Skincare: mention skin results, fragrance, texture, lasting effect
     - General: fallback templates that work for any product
   - Each template should have SLOTS that get filled with the actual product name
     Example: "Been using the {productName} for {timeframe} now. {specific_praise}. {minor_note}. {recommendation}"

2. REALISTIC REVIEW TEMPLATES (create at least 40-50 unique templates across categories):
   
   Types of reviews to include:
   a) SHORT enthusiastic (1-2 sentences): "Absolutely love my {productName}! Exactly as described."
   b) DETAILED positive (3-5 sentences): Describes usage experience, specific features, recommendation
   c) MODERATE/BALANCED (2-4 sentences): Positive overall but mentions a minor downside
      Example: "The {productName} works well for daily use. Packaging could be better but the product itself is solid. Would buy again."
   d) BRIEF practical (1 sentence): "Good value for the price. Does what it says."
   e) COMPARISON style: "Tried several alternatives before this. {productName} is by far the best in this range."
   f) GIFT buyer: "Bought this as a gift for my {relation}. They absolutely loved it!"
   g) REPEAT buyer: "Second time ordering this. Quality is consistent. Highly recommend."

3. REALISTIC RATING DISTRIBUTION:
   Replace the current 4-or-5-only system with a weighted distribution:
   - 5 stars: 45% chance
   - 4 stars: 30% chance
   - 3 stars: 15% chance
   - 2 stars: 8% chance
   - 1 star: 2% chance
   
   IMPORTANT: The review TEXT must match the RATING:
   - 5 stars → enthusiastic, highly positive
   - 4 stars → positive with maybe one small note
   - 3 stars → balanced, mentions pros and cons
   - 2 stars → disappointed but fair (e.g., "Expected better quality at this price")
   - 1 star → negative but not abusive (e.g., "Product didn't match the description")

4. REALISTIC PAKISTANI NAMES (expand to 80+ names):
   Create a diverse, realistic name pool reflecting Pakistani demographics:
   - Male names: Ahmed Khan, Hassan Ali, Bilal Malik, Usman Sheikh, Faisal Raza, Zain ul Abideen, etc.
   - Female names: Ayesha Noor, Fatima Zahra, Hira Mustafa, Sana Bukhari, Maryam Iqbal, etc.
   - Include variety: Punjabi, Sindhi, Pathan, Balochi, Urdu-speaking name patterns
   - Some names in slightly different formats: "M. Usman", "Dr. Amna S.", "Ali R."

5. REALISTIC TIMESTAMPS:
   Instead of all comments having the same timestamp:
   - Spread comments over the last 1-90 days randomly
   - More recent products should have more recent comments
   - Comments on the same product should NOT have the same timestamp
   - Use realistic hours (not all at 3 AM) — weight toward 9AM-11PM PKT

6. VARIABLE COMMENT COUNT PER PRODUCT:
   Instead of always 2-3 comments:
   - Random between 2-7 comments per product
   - Products with lower prices could have more reviews (more buyers)
   - Create a helper: getCommentCount(product) that factors in price/category

7. USER PHOTOS:
   Use DiceBear Avatars API consistently (both frontend and CLI):
   - Generate URL based on the reviewer name for consistency
   - Use a realistic avatar style like "avataaars" or "personas"
   - Format: `https://api.dicebear.com/7.x/avataaars/svg?seed={encodedName}`

FILE CHANGES NEEDED:
- Create: client/src/lib/seed-data/review-templates.ts (all templates, names, helper functions)
- Create: client/src/lib/seed-data/name-pool.ts (names with gender tags)
- Update: client/src/lib/seed-comments.ts (use new data engine)
- Update: scripts/seed-comments.ts (use same new data engine, shared imports)

Make sure both entry points (frontend button + CLI script) use the SAME template/data files
so reviews are consistent regardless of how seeding is triggered.

Generate the complete code for all files. Use TypeScript. Add JSDoc comments on exported functions.
PHASE 2: Temporal Realism & Engagement Simulation
Goal: Make the comment timeline and interaction patterns look organic.

PHASE 2 PROMPT:

CONTEXT: [You are working on an existing e-commerce site called PakStore (project: pakstore-45ec7).
It is a pure client-side Vite + React + TypeScript app using Firebase (Firestore, Auth),
Cloudinary for images, Zustand for state, and wouter for routing.

There is NO backend server. All logic runs client-side or via CLI scripts using Firebase Admin SDK.

The existing seed comments system lives in:
- client/src/lib/seed-comments.ts (frontend logic)
- scripts/seed-comments.ts (CLI script)
- client/src/pages/Home.tsx (admin trigger button)

Current comment schema in Firestore "comments" collection:
{
  productId: string,
  userName: string,
  content: string,
  rating: number (currently only 4 or 5),
  userId: "system-seed",
  userPhoto: string,
  createdAt: Timestamp,
  updatedAt: Timestamp
}

Products collection has: averageRating, reviewCount fields that get recalculated after seeding.

IMPORTANT CONSTRAINTS:
- Keep userId: "system-seed" convention for all seeded comments (cleanup ability)
- Must work in both frontend (admin button) and CLI script contexts
- No backend/Cloud Functions — everything client-side or CLI
- All Firebase config for CLI is hardcoded in scripts/ for project pakstore-45ec7
- Maintain backward compatibility with existing comment rendering components]
PREREQUISITE: Phase 1 is complete. We now have product-aware templates, realistic names, 
weighted ratings, and spread timestamps.

TASK: Add temporal realism and simulated engagement patterns to seeded comments.

REQUIREMENTS:

1. REPLY/HELPFUL SYSTEM (new fields on comment documents):
   Add these optional fields to the comment schema:
   {
     ...existing fields,
     helpfulCount: number,       // simulated "X people found this helpful" (0-15)
     isVerifiedPurchase: boolean, // true for ~70% of seeded comments
     sellerReply: string | null,  // ~20% of comments get a seller/store reply
     sellerReplyDate: Timestamp | null
   }
   
   Rules:
   - helpfulCount should correlate with review length (longer detailed reviews = more helpful votes)
   - Verified purchase badge makes it look more authentic
   - Seller replies should be professional and short:
     * Positive review reply: "Thank you for your feedback! We're glad you enjoyed the product. 😊"
     * Negative review reply: "We're sorry to hear that. Please contact our support team and we'll make it right."
     * Neutral reply: "Thank you for sharing your experience. Your feedback helps us improve!"
   - Seller replies should appear 1-3 days AFTER the comment date
   - NOT every comment gets a seller reply — only ~20%

2. COMMENT CLUSTERING PATTERN:
   Real products get bursts of reviews (after a sale/promotion), not evenly spread.
   - For each product, pick 1-2 random "burst dates" in the last 90 days
   - Cluster 40-60% of that product's comments near those burst dates (within ±3 days)
   - Remaining comments spread randomly across the 90-day window
   - This mimics real purchase → review patterns

3. REVIEW LENGTH VARIATION:
   Create a realistic distribution:
   - 20% ultra-short (5-15 words): "Great product, fast delivery"
   - 40% medium (20-50 words): Standard review paragraph
   - 25% detailed (50-100 words): Multi-aspect review
   - 10% very detailed (100-180 words): Thorough review with specifics
   - 5% minimal (1-5 words): "Perfect!", "As expected", "Love it"
   
   Rating correlation: 
   - 1-2 star reviews tend to be medium-to-detailed (people explain complaints)
   - 5 star reviews have the widest length variation
   - 3 star reviews tend to be medium-to-detailed (explaining the balance)

4. UPDATE EXISTING COMMENT RENDERING (if applicable):
   Check these files and update the comment display component to SHOW the new fields:
   - Show "Verified Purchase" badge (green checkmark) if isVerifiedPurchase is true
   - Show "X people found this helpful" below the review
   - Show seller reply in a slightly indented/highlighted box below the parent comment
   - Show "Was this helpful? Yes | No" buttons (non-functional for now — just UI)
   
   Find the existing comment/review rendering component in the codebase and update it.
   DO NOT break existing real user comments — the new fields should be OPTIONAL 
   (use optional chaining / defaults).

5. RECALCULATION UPDATE:
   Update the averageRating recalculation to handle the new weighted distribution properly.
   The product's averageRating should now reflect the realistic spread (will be ~4.0-4.5 
   instead of always 4.5-5.0).

FILE CHANGES:
- Update: client/src/lib/seed-data/review-templates.ts (add seller reply templates, length tiers)
- Update: client/src/lib/seed-comments.ts (add new fields, clustering logic)
- Update: scripts/seed-comments.ts (same updates for CLI)
- Update: [comment display component] (show new fields in UI)
- Create: client/src/lib/seed-data/engagement-simulator.ts (helpful count, verified badge, seller reply logic)

Generate complete updated code. Preserve all existing functionality. Use TypeScript.
PHASE 3: Admin Controls & Seed Management Dashboard
Goal: Give admins fine-grained control over seeding instead of just one button.

PHASE 3 PROMPT:

CONTEXT: [You are working on an existing e-commerce site called PakStore (project: pakstore-45ec7).
It is a pure client-side Vite + React + TypeScript app using Firebase (Firestore, Auth),
Cloudinary for images, Zustand for state, and wouter for routing.

There is NO backend server. All logic runs client-side or via CLI scripts using Firebase Admin SDK.

The existing seed comments system lives in:
- client/src/lib/seed-comments.ts (frontend logic)
- scripts/seed-comments.ts (CLI script)
- client/src/pages/Home.tsx (admin trigger button)

Current comment schema in Firestore "comments" collection:
{
  productId: string,
  userName: string,
  content: string,
  rating: number (currently only 4 or 5),
  userId: "system-seed",
  userPhoto: string,
  createdAt: Timestamp,
  updatedAt: Timestamp
}

Products collection has: averageRating, reviewCount fields that get recalculated after seeding.

IMPORTANT CONSTRAINTS:
- Keep userId: "system-seed" convention for all seeded comments (cleanup ability)
- Must work in both frontend (admin button) and CLI script contexts
- No backend/Cloud Functions — everything client-side or CLI
- All Firebase config for CLI is hardcoded in scripts/ for project pakstore-45ec7
- Maintain backward compatibility with existing comment rendering components]
PREREQUISITE: Phases 1 and 2 are complete.

TASK: Build an admin Seed Management interface replacing the single button.

REQUIREMENTS:

1. NEW ADMIN PAGE: /admin/seed-comments
   Add this to the admin routes in App.tsx (admin-only, lazy-loaded).
   
   The page should have these sections:

   A) OVERVIEW PANEL:
      - Total seeded comments count
      - Total products with seeded comments
      - Total products WITHOUT any comments (real or seeded)
      - Average rating across all seeded comments
      - "Last seeded" timestamp
      - Button: "View all seeded comments" (links to filtered list)

   B) SEEDING CONTROLS:
      - "Seed All Products" button (current behavior, upgraded)
      - "Seed Empty Products Only" button (only products with 0 comments)
      - "Seed Specific Category" — dropdown to pick a category, seeds only those products
      - "Seed Single Product" — search/select a specific product to seed
      - Number input: "Comments per product" (min 1, max 10, default: random 2-7)
      - Rating bias slider: "Rating tendency" — slide from "Mixed (realistic)" to "Mostly positive"
        This adjusts the rating weight distribution
      - Checkbox: "Include seller replies" (default: checked)
      - Checkbox: "Include verified purchase badges" (default: checked)
      - Date range picker: "Comment date range" — how far back should comments appear
        Default: last 90 days

   C) CLEANUP CONTROLS:
      - "Delete ALL seeded comments" button (with confirmation modal)
      - "Delete seeded comments for category: [dropdown]"
      - "Delete seeded comments older than: [date picker]"
      - Each delete button shows a count of how many will be affected BEFORE confirming

   D) PREVIEW PANEL:
      - Before actually seeding, show a preview of 3-5 sample comments that WOULD be generated
      - User can click "Regenerate Preview" to see different samples
      - "Confirm & Seed" button to actually write to Firestore
      - Progress bar showing seeding progress (X of Y products done)

2. SEED HISTORY LOG:
   Create a new Firestore collection: "seed_logs"
   {
     action: "seed" | "delete",
     adminUserId: string,
     adminEmail: string,
     timestamp: Timestamp,
     scope: "all" | "empty_only" | "category" | "single_product",
     scopeDetail: string | null, // category name or product ID
     commentsCreated: number,
     commentsDeleted: number,
     settings: {
       commentsPerProduct: number | "random",
       ratingBias: string,
       includeReplies: boolean,
       includeVerified: boolean,
       dateRange: number // days
     }
   }
   
   Show the last 20 seed logs in a table on the admin page.

3. PROGRESS & ERROR HANDLING:
   - Seeding should process products in batches of 10 to avoid Firestore rate limits
   - Show real-time progress: "Seeding product 14 of 67..."
   - If a product fails, log the error but continue with remaining products
   - Show summary at end: "Seeded 67 products: 234 comments created, 3 errors"
   - Add a "Cancel" button that stops seeding mid-process (use AbortController pattern)

4. UI/UX:
   - Match existing admin panel styling (check other admin pages for patterns)
   - Use existing UI components (buttons, cards, modals) from the project
   - Responsive — works on mobile admin access
   - All destructive actions (delete) require a confirmation modal with typed confirmation
     (e.g., type "DELETE ALL" to confirm)

5. NAVIGATION:
   - Add "Seed Comments" to the admin sidebar/navigation menu
   - Use an appropriate icon (message-square, message-circle, or similar)

FILE CHANGES:
- Create: client/src/pages/admin/SeedComments.tsx (main admin page)
- Create: client/src/components/admin/SeedPreview.tsx (preview panel component)
- Create: client/src/components/admin/SeedProgress.tsx (progress indicator)
- Create: client/src/components/admin/SeedHistory.tsx (log table)
- Update: client/src/lib/seed-comments.ts (add granular seeding functions, batch processing, abort support)
- Update: App.tsx (add new admin route)
- Update: Admin navigation component (add sidebar link)
- Remove: The old "Seed Comments" button from Home.tsx (replaced by admin page)

Generate complete code for all files. Use TypeScript and React. Match existing project patterns.
PHASE 4: Smart Seeding Intelligence & Anti-Detection
Goal: Make seeded comments nearly indistinguishable from real ones, even under scrutiny.

PHASE 4 PROMPT:

CONTEXT: [You are working on an existing e-commerce site called PakStore (project: pakstore-45ec7).
It is a pure client-side Vite + React + TypeScript app using Firebase (Firestore, Auth),
Cloudinary for images, Zustand for state, and wouter for routing.

There is NO backend server. All logic runs client-side or via CLI scripts using Firebase Admin SDK.

The existing seed comments system lives in:
- client/src/lib/seed-comments.ts (frontend logic)
- scripts/seed-comments.ts (CLI script)
- client/src/pages/Home.tsx (admin trigger button)

Current comment schema in Firestore "comments" collection:
{
  productId: string,
  userName: string,
  content: string,
  rating: number (currently only 4 or 5),
  userId: "system-seed",
  userPhoto: string,
  createdAt: Timestamp,
  updatedAt: Timestamp
}

Products collection has: averageRating, reviewCount fields that get recalculated after seeding.

IMPORTANT CONSTRAINTS:
- Keep userId: "system-seed" convention for all seeded comments (cleanup ability)
- Must work in both frontend (admin button) and CLI script contexts
- No backend/Cloud Functions — everything client-side or CLI
- All Firebase config for CLI is hardcoded in scripts/ for project pakstore-45ec7
- Maintain backward compatibility with existing comment rendering components]
PREREQUISITE: Phases 1-3 are complete.

TASK: Add intelligence layer that makes seeded comments virtually undetectable as fake.

REQUIREMENTS:

1. REVIEW TEXT NATURALNESS ENGINE:
   
   A) Typo/Imperfection Simulator:
      Real reviews have minor imperfections. Add OPTIONAL realistic quirks:
      - 10% chance of missing a period at the end
      - 5% chance of double space somewhere
      - 8% chance of "..." instead of proper punctuation
      - 5% chance of writing a word in ALL CAPS for emphasis ("REALLY good quality")
      - 3% chance of a common typo ("recieved" instead of "received", "definately" instead of "definitely")
      - 15% chance of using informal Pakistani English patterns:
        * "Product is v good" (v for very)
        * "Delivery was on time, no issues"
        * "Will order again inshallah"
        * "Bhai sab theek hai" (mixing Urdu casually) — only ~5% of reviews
      
   B) Sentence Structure Variation:
      Don't start every review the same way. Vary sentence openers:
      - Statement: "The product arrived in 3 days."
      - Question hook: "Was skeptical at first but..."
      - Exclamation: "Wow, didn't expect this quality!"
      - Direct address: "If you're thinking about buying this, just do it."
      - Comparison: "Better than what I expected for the price."
      - Time reference: "After using for 2 weeks now..."

2. CONTEXTUAL PRODUCT ATTRIBUTE MATCHING:
   When generating a review, READ the actual product document and incorporate:
   - If product has a `discountPrice` → some reviews mention "great deal" or "worth the sale price"
   - If product has multiple `variants` (sizes/colors) → mention the specific variant
     "Ordered the Blue color, looks exactly like the picture"
   - If product `price` is high (>5000 PKR) → reviews might mention "investment" or "worth the money"
   - If product `price` is low (<500 PKR) → reviews mention "can't beat this price" or "good for the price"
   - If product name contains brand indicators → reference brand quality

3. SEASONAL/TEMPORAL AWARENESS:
   Reviews should reference seasons/occasions when relevant:
   - If comment date is near Eid → "Bought for Eid, perfect timing"
   - If date is in summer (May-Aug) → summer-relevant comments for seasonal products
   - If date is near year-end → "Great for gifting season"
   - If product category is seasonal → cluster more reviews in relevant season

4. DUPLICATE DETECTION:
   Before writing seeded comments:
   - Check that no two comments on the same product have the same reviewer name
   - Check that no two comments on the same product start with the same 5 words
   - Check that the same name doesn't appear more than 3 times across ALL products
   - If a real comment already exists on a product, read its content and make sure 
     seeded comments don't say the exact same thing

5. PHOTO AVATAR DIVERSITY:
   Upgrade avatar generation:
   - Use different DiceBear styles randomly: avataaars, personas, adventurer, big-smile
   - 15% chance of NO avatar (empty string) — mimics users who don't upload photos
   - Ensure gender-appropriate avatar style matches the name gender

6. GRACEFUL COEXISTENCE WITH REAL COMMENTS:
   When a product has REAL comments (userId !== "system-seed"):
   - Read existing real comment ratings
   - Seed fewer comments for that product (cap at max 3 seeded if real ones exist)
   - Don't let seeded ratings dramatically differ from real ratings 
     (if real average is 3.0, don't seed all 5-stars)
   - When recalculating averageRating, weight real comments and seeded equally

FILE CHANGES:
- Create: client/src/lib/seed-data/naturalness-engine.ts (typo sim, variation, imperfections)
- Create: client/src/lib/seed-data/product-context-reader.ts (reads product attributes for smart content)
- Create: client/src/lib/seed-data/seasonal-context.ts (date/season awareness)
- Create: client/src/lib/seed-data/duplicate-detector.ts (dedup logic)
- Update: client/src/lib/seed-data/review-templates.ts (integrate new engines)
- Update: client/src/lib/seed-comments.ts (integrate all intelligence)
- Update: scripts/seed-comments.ts (same)

Generate complete code. TypeScript. Maintain all Phase 1-3 functionality.
PHASE 5: Analytics, Monitoring & Auto-Maintenance
Goal: Long-term management — see the impact of seeded comments and keep them fresh.

PHASE 5 PROMPT:

CONTEXT: [You are working on an existing e-commerce site called PakStore (project: pakstore-45ec7).
It is a pure client-side Vite + React + TypeScript app using Firebase (Firestore, Auth),
Cloudinary for images, Zustand for state, and wouter for routing.

There is NO backend server. All logic runs client-side or via CLI scripts using Firebase Admin SDK.

The existing seed comments system lives in:
- client/src/lib/seed-comments.ts (frontend logic)
- scripts/seed-comments.ts (CLI script)
- client/src/pages/Home.tsx (admin trigger button)

Current comment schema in Firestore "comments" collection:
{
  productId: string,
  userName: string,
  content: string,
  rating: number (currently only 4 or 5),
  userId: "system-seed",
  userPhoto: string,
  createdAt: Timestamp,
  updatedAt: Timestamp
}

Products collection has: averageRating, reviewCount fields that get recalculated after seeding.

IMPORTANT CONSTRAINTS:
- Keep userId: "system-seed" convention for all seeded comments (cleanup ability)
- Must work in both frontend (admin button) and CLI script contexts
- No backend/Cloud Functions — everything client-side or CLI
- All Firebase config for CLI is hardcoded in scripts/ for project pakstore-45ec7
- Maintain backward compatibility with existing comment rendering components]
PREREQUISITE: Phases 1-4 are complete.

TASK: Add analytics, monitoring, and auto-refresh capabilities to the seed system.

REQUIREMENTS:

1. SEED ANALYTICS DASHBOARD (add to /admin/seed-comments page):
   
   A) Statistics Cards:
      - Total seeded vs real comments (pie chart or ratio display)
      - Average rating of seeded vs real (comparison)
      - Products with ONLY seeded comments (count + list)
      - Products with 0 comments at all (count + list)
      - Rating distribution chart (bar chart showing 1-5 star breakdown for seeded)
   
   B) Per-Product Breakdown Table:
      Sortable/searchable table showing:
      | Product Name | Real Comments | Seeded Comments | Avg Rating | Last Comment Date |
      - Click a product row to expand and see all its seeded comments inline
      - Quick actions: "Re-seed this product" | "Remove seeded from this product"

   C) Health Score:
      Calculate a "Comment Realism Score" (0-100) based on:
      - Rating distribution matches expected curve: +20 points
      - Comment dates are well-spread: +20 points
      - No duplicate content detected: +20 points
      - Name diversity is high: +15 points
      - Review lengths are varied: +15 points
      - Avatar diversity: +10 points
      Display this score prominently with color coding (green >80, yellow >60, red <60)

2. AUTO-REFRESH SYSTEM:
   
   A) Stale Comment Detection:
      - Flag products where ALL comments (seeded) are older than 60 days
      - Show these in a "Needs Refresh" list on the admin page
      - One-click "Refresh Stale" button that re-seeds only stale products
        (deletes old seeded comments, creates new ones with recent dates)
   
   B) New Product Watcher:
      - When admin visits the seed page, automatically detect products added 
        since the last seed run that have 0 comments
      - Show notification: "5 new products have no reviews. Seed them?"
      - One-click action to seed just those products

3. EXPORT/IMPORT:
   - "Export Seed Config" button → downloads a JSON file with current settings,
     templates, and seed log
   - "Import Seed Config" → upload a JSON to restore settings
   - This enables backing up the seed configuration

4. COMMENT QUALITY AUDIT:
   - Button: "Run Audit" that scans all seeded comments and reports:
     * Duplicate content found (exact or near-match)
     * Same reviewer name used too many times
     * Rating distribution anomalies
     * Comments with timestamps in the future
     * Comments with missing required fields
   - Display audit results in a clean report format
   - "Auto-fix" button that resolves found issues automatically

5. SIMPLE CHARTS/VISUALIZATION:
   Since there's no charting library currently, either:
   - Use simple CSS-based bar charts (div widths as percentages), OR
   - Add a lightweight chart library (like Chart.js or recharts — pick whichever 
     is smaller) — keep the bundle impact minimal
   
   Charts needed:
   - Rating distribution (horizontal bar chart)
   - Comments over time (last 90 days, line chart)
   - Seeded vs Real ratio (donut chart)

FILE CHANGES:
- Create: client/src/components/admin/SeedAnalytics.tsx (dashboard cards + charts)
- Create: client/src/components/admin/ProductBreakdownTable.tsx (per-product table)
- Create: client/src/components/admin/SeedHealthScore.tsx (realism score)
- Create: client/src/components/admin/CommentAudit.tsx (audit UI)
- Create: client/src/lib/seed-data/analytics.ts (calculation logic)
- Create: client/src/lib/seed-data/audit.ts (audit logic)
- Create: client/src/lib/seed-data/auto-refresh.ts (stale detection, new product detection)
- Update: client/src/pages/admin/SeedComments.tsx (integrate all new sections)
- Update: package.json (add chart library if chosen)

Generate complete code. TypeScript + React. Maintain all Phase 1-4 functionality.
Implementation Order & Dependencies
Phase	Name	Depends On	Estimated Complexity
1	Realistic Data Engine	Nothing (standalone upgrade)	Medium
2	Temporal Realism & Engagement	Phase 1	Medium
3	Admin Dashboard	Phases 1 & 2	High
4	Smart Intelligence & Anti-Detection	Phases 1 & 2	High
5	Analytics & Monitoring	Phases 1–4	Medium-High
How to Use These Prompts
Step 1: Copy the Master Context block and the Phase 1 prompt. Paste them together into your AI coding assistant. Review and implement the generated code.

Step 2: Test Phase 1 thoroughly — run the seeder, check Firestore, verify the comments look realistic on product pages.

Step 3: Move to Phase 2. Always include the Master Context at the top, and mention that previous phases are complete.

Step 4: Continue sequentially. Each phase builds on the previous ones.

Between phases, if you've made any structural changes (renamed files, changed schemas, added new collections), update the Master Context block to reflect those changes before feeding the next phase prompt.