You are helping me implement a robust Cloudinary upload system in my existing project.

Project context:

Frontend: React 18 + Vite + TypeScript
State: TanStack Query, Zustand
Backend: Firebase (Firestore + Auth), no custom Node server
Images: currently using a single Cloudinary account with unsigned uploads from the frontend
Env vars: VITE_CLOUDINARY_*
Goal: I want to use two different Cloudinary accounts (Account A and Account B) in this same frontend project.
Behavior I want:

By default, all uploads go to Account A.
If Account A is out of quota / storage (or returns a “quota exceeded” error), uploads for that request should fallback to Account B.
The app should always store full image URLs (or at least cloud_name + public_id) in Firestore so images from both accounts can be displayed normally.
All Cloudinary secrets must remain secure (no api_secret in frontend).
Requirements
Two Cloudinary configurations

Add support for two Cloudinary accounts using environment variables:

Account A:
VITE_CLOUDINARY_A_CLOUD_NAME
VITE_CLOUDINARY_A_UPLOAD_PRESET
Account B:
VITE_CLOUDINARY_B_CLOUD_NAME
VITE_CLOUDINARY_B_UPLOAD_PRESET
Create a small config file (e.g. src/config/cloudinary.ts) exporting these two configs:

Copyexport const CLOUDINARY_A = {
  cloudName: import.meta.env.VITE_CLOUDINARY_A_CLOUD_NAME!,
  uploadPreset: import.meta.env.VITE_CLOUDINARY_A_UPLOAD_PRESET!,
};

export const CLOUDINARY_B = {
  cloudName: import.meta.env.VITE_CLOUDINARY_B_CLOUD_NAME!,
  uploadPreset: import.meta.env.VITE_CLOUDINARY_B_UPLOAD_PRESET!,
};
Upload wrapper function (frontend)

Create a reusable upload utility (e.g. src/lib/uploadImage.ts) with this API:

Copyexport async function uploadImage(file: File): Promise<{
  url: string;
  publicId: string;
  cloudName: string;
}>;
Implementation details:

First, try uploading to Account A using unsigned upload and its preset.
If the upload fails with an error that looks like quota / rate / storage limit (you will need to check typical Cloudinary error responses), then:
Log or handle the error
Retry the upload once with Account B.
If the error is something else (network error, invalid file type, etc.), just throw it and do not fallback.
Return the final url, publicId, and cloudName used.
Use the standard unsigned upload endpoint from the browser:

Copyhttps://api.cloudinary.com/v1_1/<cloud_name>/image/upload
Error handling helper

Create a small helper function inside the same file:

Copyfunction isCloudinaryQuotaError(err: unknown): boolean;
That:

Examines the error / response from Cloudinary
Returns true if it likely indicates:
storage limit exceeded
over quota
rate limit / plan limit
Otherwise returns false.
Implement this using realistic Cloudinary error patterns (e.g., HTTP status codes, error.code, error.message content).

Usage in React components

Show me a TypeScript example component ImageUploader.tsx that:

Accepts a <input type="file" />
On file select, calls uploadImage(file)
Shows:
loading state
success with preview of the returned url
error message on failure
Logs which cloudName was ultimately used, so I can verify fallback behavior.
Firestore integration (high-level)

Show a small example function for saving the uploaded image info to Firestore:

Copytype StoredImage = {
  url: string;
  publicId: string;
  cloudName: string;
  createdAt: number;
};

async function saveImageMetadataToFirestore(image: StoredImage): Promise<void>;
And how a product document could look like in Firestore that supports images from both accounts:

Copytype Product = {
  id: string;
  name: string;
  price: number;
  image: {
    url: string;
    publicId: string;
    cloudName: string;
  };
  // other fields...
};
Display images from both accounts

Provide a simple React component ProductImage.tsx that:

Receives image: { url: string; publicId: string; cloudName: string }
Renders img using image.url
Optionally shows how I could construct a Cloudinary transformation URL using cloudName + publicId if I don’t store full URL.
Security notes

Assume all uploads are unsigned from frontend using upload presets.
Do not use or expose api_secret anywhere.
Mention where to configure the upload presets in Cloudinary (high-level description is enough).