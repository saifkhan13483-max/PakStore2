rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /comments/{commentId} {
      allow read: if true;
      allow create: if true; // In production, change to request.auth != null
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
